spring:
  application:
    name: gateway

  output:
    ansi:
      enabled: always

  server:
    port: 8080

  management:
    endpoints:
      web:
        exposure:
          include: health, info
